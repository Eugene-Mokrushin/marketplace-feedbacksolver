import firebaseui from "firebaseui";

interface SignupProps {
  toSignedup: () => void;
}

const Signup = ({ toSignedup }: SignupProps) => {
  return <div style={{ minWidth: "100%" }}></div>;
};

export default Signup;
